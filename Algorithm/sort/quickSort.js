// // 1. pivot을 설정한다.
// // 2. 왼쪽으로 pivot 보다 작은 값을, 오른쪽으로 pivot 보다 큰 값을 놓는다.
// // 3. pivot을 기준으로 왼쪽, 오른쪽을 나눈다.
// // 4. pivot이 해당 파티션에서 가장 작은 값 or 가장 큰 값일 때, 최악의 경우가 나온다. O(n^2)

// array 값이 함수 스코프 내에서 계속 활용된다.
